using UnityEngine;

public class MOINE : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        Classesroues.s_MoineIsUnlocked = true;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
